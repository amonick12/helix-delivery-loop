---
name: status
description: >
  Show delivery loop status: board state, open PRs, active agents, stuck cards,
  dead letters, failures, throughput, and next dispatch. Use /status to check pipeline health.
---

Use the `/status` command to invoke this skill. Runs `status.sh` and presents results.
