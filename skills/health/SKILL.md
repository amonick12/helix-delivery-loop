---
name: health
description: >
  Pipeline health check. Scans for label mismatches between PRs and issues,
  missing gates.json files, stale state entries, PR conflicts, epic cards
  stuck in the pipeline, and reports the next dispatch decision.
---

Use the `/health` command to invoke this skill.
