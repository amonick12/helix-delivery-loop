---
name: unstick
description: >
  Diagnose and fix a stuck card. Checks for stale rework_target, missing
  gates.json, label mismatches, missing worktrees, and PR conflicts.
  Applies fixes automatically and re-runs the dispatcher to verify.
---

Use the `/unstick <card>` command to invoke this skill.
