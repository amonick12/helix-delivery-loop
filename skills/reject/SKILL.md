---
name: reject
description: >
  Reject a PR with feedback. Posts a rejection comment, removes all
  approval labels, moves the card back to In Progress, and routes
  to Builder for rework. Use when a PR needs changes.
---

Use the `/reject <pr> <reason>` command to invoke this skill.
